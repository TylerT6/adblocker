https://github.com/TylerT6/food-order-1
@@ -1,4 +1,4 @@
import { useState } from "react";
import { useState } from 'react';

interface InPlaceCurrencyInputProps {
  value: string;
@@ -10,11 +10,10 @@ export default function InPlaceCurrencyInput(props: InPlaceCurrencyInputProps) {

  const handleInputBlur = () => {
    setInputType('text');
  }
  };
  const handleInputFocus = () => {
    setInputType('number');
  }

  };

  const formatValue = () => {
    if (inputType === 'text') {
@@ -23,20 +22,19 @@ export default function InPlaceCurrencyInput(props: InPlaceCurrencyInputProps) {
    } else {
      return props.value;
    }
  }
  };

  const handleKeyPress = (event: React.KeyboardEvent<HTMLInputElement>) => {
    const charCode = event.charCode;
    if (charCode < 48 || charCode > 57) {
      event.preventDefault();

    }
  };

  return (
    <div>
      <input
        className=""
        className=''
        min={0}
        onFocus={handleInputFocus}
        onBlur={handleInputBlur}
@@ -47,8 +45,13 @@ export default function InPlaceCurrencyInput(props: InPlaceCurrencyInputProps) {
        }}
        type={inputType}
        value={formatValue()}
        style={{ backgroundColor: 'transparent', border: 'none', outline: 'none', boxShadow: 'none', }}
        style={{
          backgroundColor: 'transparent',
          border: 'none',
          outline: 'none',
          boxShadow: 'none',
        }}
      />
    </div>
  );
}
}
